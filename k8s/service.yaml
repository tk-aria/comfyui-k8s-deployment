apiVersion: v1
kind: Service
metadata:
  name: comfyui-worker
  labels:
    app: comfyui-worker
spec:
  type: ClusterIP
  selector:
    app: comfyui-worker
  ports:
  - name: http
    port: 80
    targetPort: 8188
    protocol: TCP
