apiVersion: apps/v1
kind: Deployment
metadata:
  name: comfyui-z-image-turbo
  namespace: default
  labels:
    app: comfyui-z-image-turbo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comfyui-z-image-turbo
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: comfyui-z-image-turbo
    spec:
      containers:
        - name: comfyui-z-image-turbo
          image: ghcr.io/tk-aria/comfyui-z-image-turbo:v1.0.0-z-image-turbo
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-c"]
          args:
            - |
              cd /comfyui && python -u main.py --listen 0.0.0.0 --port 8188 --disable-auto-launch --disable-metadata
          ports:
            - name: http
              containerPort: 8188
              protocol: TCP
          env:
            - name: COMFYUI_OUTPUT_PATH
              value: /comfyui/output
          resources:
            requests:
              cpu: "10m"
              memory: "2Gi"
            limits:
              cpu: "4"
              memory: "16Gi"
          volumeMounts:
            - name: output-volume
              mountPath: /comfyui/output
          readinessProbe:
            httpGet:
              path: /
              port: 8188
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8188
            initialDelaySeconds: 120
            periodSeconds: 30
            failureThreshold: 5
          tty: true
      imagePullSecrets:
        - name: ghcr-credentials
      nodeSelector:
        kubernetes.io/os: linux
      volumes:
        - name: output-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: comfyui-z-image-turbo
  namespace: default
  labels:
    app: comfyui-z-image-turbo
spec:
  type: ClusterIP
  ports:
    - port: 8188
      targetPort: 8188
      protocol: TCP
      name: http
  selector:
    app: comfyui-z-image-turbo
